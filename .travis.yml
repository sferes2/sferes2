language: cpp
compiler:
  - gcc
  - clang
addons:
  apt:
    packages:
    - libboost-dev
    - libboost-test-dev 
    - libeigen3-dev
    - libtbb-dev

install: 


# Change this to your needs
script:
 dpkg -L libboost-serialization1.54-dev 
 ./waf configure && ./waf -v