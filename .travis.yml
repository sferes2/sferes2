language: cpp

os:
  - linux

sudo: required

dist: trusty

compiler:
  - gcc
  - clang
addons:
  apt:
    packages:
    - libboost1.58-all-dev
    - libeigen3-dev
    - libtbb-dev

install:


# Change this to your needs
script:
  - ./waf configure
  - ./waf --tests=yes -v
